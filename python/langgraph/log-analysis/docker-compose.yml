name: log-analysis

services:
  log-analysis:
    container_name: log-analysis
    build:
      context: .
    image: log-analysis
    volumes:
      - .:/app
    ports:
      - "2024:2024"
    env_file: .env
    command: ["langgraph", "dev", "--host", "0.0.0.0", "--port", "2024", "--no-browser"]
