# Strategy: With Planner

> **Removed.** This strategy (`src/langgraph_mcp/with_planner/`) has been removed from the project.
>
> The current agents are `agent` (`src/deepagent_mcp/`) and `simple_agent` (`src/simple_agent/`). See [index.md](./index.md) for details.
