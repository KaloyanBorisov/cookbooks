services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: mcp-agent-template
    restart: unless-stopped
    env_file:
      - .env
    environment:
      DEBUG: ${DEBUG:-0}
    volumes:
      - ./src:/app/src
    ports:
      - "2024:2024"
      - "2025:2025"
